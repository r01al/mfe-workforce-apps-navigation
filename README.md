# Workforce Navigation Remote

Independent navigation micro-frontend. It exposes `./Navigation` from the `navigationMfe` container and serves `remoteEntry.js` with CORS enabled.

```bash
npm install
npm run dev
npm run typecheck
npm run build
npm start
```
